quantization:
  method: jacob # jacob | disable

  weight:
    num_bits: 8
    axis: per_channel # per_tensor | per_channel
    calib_method: max
  
  activation:
    num_bits: 8
    axis: per_tensor # per_tensor | per_channel
    calib_method: max

  calibration:
    num_batch: 2
    modal: rgbd
